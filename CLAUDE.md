# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Overview

This is a GitHub Pages site (`shistone.github.io`). It is currently empty — no static site generator, build tooling, or framework has been set up yet.

## GitHub Pages

The `main` branch is the deployment branch. Pushing to `main` publishes the site at `https://shistone.github.io`.

No build step is required for plain HTML/CSS/JS. If a static site generator (Jekyll, Hugo, Eleventy, etc.) is added later, update this file with build commands and local preview instructions.
